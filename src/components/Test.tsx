import React from 'react'
// import { Deadline } from '../models/Deadline'

const Test = (
  // items: Array<Deadline> | any
) => {
  // const tmpItems = items.concat()
  // const nonFinishedCount: Array<Deadline> | any = items?.filter((item: Deadline | any) => item.finished === false)
  // console.log(nonFinishedCount)
  return (
    <>
      <p>test</p>
    </>
  )
}

export default Test